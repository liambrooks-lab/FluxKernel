# FluxKernel Workspace
# This directory is the sandboxed environment where FluxKernel reads,
# writes, and executes files on your behalf.
# Everything inside here is agent-controlled.

welcome = "FluxKernel Workspace Active"
